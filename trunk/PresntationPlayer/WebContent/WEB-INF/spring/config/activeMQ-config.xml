<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:amq="http://activemq.apache.org/schema/core" 
	xmlns:jms="http://www.springframework.org/schema/jms"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
http://activemq.apache.org/schema/core http://activemq.apache.org/schema/core/activemq-core-5.4.0.xsd
http://www.springframework.org/schema/jms http://www.springframework.org/schema/jms/spring-jms-3.0.xsd">

	<amq:connectionFactory id="amqConnectionFactory" brokerURL="${active.mq.broker.url}"  />
	
	<bean id="connectionFactory" class="org.springframework.jms.connection.CachingConnectionFactory">
		<constructor-arg ref="amqConnectionFactory" />
		<property name="exceptionListener" ref="jobExceptionListener" />
		<property name="sessionCacheSize" value="100" />
	</bean>

	<bean id="jmsTemplate" class="org.springframework.jms.core.JmsTemplate">
		<constructor-arg ref="connectionFactory" />
		<property name="defaultDestinationName" value="${active.mq.queue.name}"/>
	</bean>

	<jms:listener-container concurrency="5">
		<jms:listener id="JobConsumer" destination="${active.mq.queue.name}" ref="jobConsumer" />
	</jms:listener-container>
	
	<bean id="jobExceptionListener" class="com.school.upload.job.JobExceptionListener"/>
	
	<bean id="jobConsumer" class="com.school.upload.job.JobConsumer">
		<property name="converter" ref="presentationConverter"></property>
	</bean>
	
	<bean id="jobSenderImpl" class="com.school.upload.job.JobSenderImpl">
		<constructor-arg type="org.springframework.jms.core.JmsTemplate" ref="jmsTemplate" />
	</bean>
	
</beans>